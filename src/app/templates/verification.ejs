<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Verify Email</title>
    <style>
      body { font-family: Arial, sans-serif; background: #f4f7fb; padding: 24px; color: #132a13; }
      .card { max-width: 560px; margin: 0 auto; background: #fff; border-radius: 12px; padding: 24px; border: 1px solid #e4ebe4; }
      .brand { color: #2d6a4f; font-weight: 700; font-size: 20px; margin-bottom: 8px; }
      .btn { display: inline-block; margin-top: 14px; background: #2d6a4f; color: #fff !important; text-decoration: none; padding: 12px 18px; border-radius: 8px; }
      .hint { color: #4f5d75; font-size: 13px; margin-top: 16px; }
    </style>
  </head>
  <body>
    <div class="card">
      <div class="brand">EcoSpark Hub</div>
      <h2>Verify your email</h2>
      <p>Hello <b><%= name %></b>,</p>
      <p>Click the button below to verify your account and continue:</p>
      <a href="<%= verifyUrl %>" class="btn">Verify Email</a>
      <p class="hint">If you didn't request this, you can safely ignore this email.</p>
    </div>
  </body>
</html>
